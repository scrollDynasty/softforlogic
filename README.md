# 🚛 Schneider FreightPower Load Parser

Высокопроизводительный автоматический парсер для мониторинга грузов на портале Schneider FreightPower с отправкой уведомлений в Telegram.

## 🚀 Особенности

- **⚡ Высокая производительность**: Асинхронная обработка, пакетные операции, оптимизированный браузер
- **🔍 Умный мониторинг**: Гибридная стратегия с WebSocket и быстрым сканированием
- **💰 Фильтрация прибыльности**: Автоматический расчет и фильтрация прибыльных грузов
- **📱 Telegram уведомления**: Красивые уведомления с детальной информацией
- **🛡️ Надежность**: Система восстановления, обработка ошибок, автоматические бэкапы
- **📊 Мониторинг**: Детальная статистика и отчеты о производительности
- **🔧 Адаптивность**: Динамическое изменение параметров на основе производительности

## 📋 Требования

- Python 3.11+
- Linux/Windows/macOS
- Интернет-соединение
- Telegram Bot Token
- Аккаунт Schneider FreightPower

## 🛠️ Установка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd schneider_parser
```

### 2. Создание виртуального окружения
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
# или
.venv\Scripts\activate  # Windows
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Установка Playwright
```bash
playwright install chromium
```

### 5. Настройка конфигурации

#### Создание файла .env
```bash
cp config/env_template.txt config/.env
```

#### Редактирование .env файла
```env
# Telegram
TELEGRAM_BOT_TOKEN=your_actual_bot_token_here
TELEGRAM_CHAT_ID=your_actual_chat_id_here

# Schneider (опционально, если нужно переопределить)
SCHNEIDER_EMAIL=primecargoload@gmail.com
SCHNEIDER_PASSWORD=Primecargo2024$

# Опциональные настройки
DEBUG_MODE=false
ENABLE_SCREENSHOTS=false
LOG_LEVEL=INFO
```

### 6. Настройка Telegram Bot

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Добавьте бота в нужную группу/канал
4. Получите Chat ID группы/канала
5. Обновите файл `.env`

## 🚀 Запуск

### Тестовый режим
```bash
python main.py --test
```

### Обычный запуск
```bash
python main.py
```

### Запуск с отладкой
```bash
python main.py --debug
```

### Запуск с кастомной конфигурацией
```bash
python main.py --config path/to/config.json
```

## ⚙️ Конфигурация

Основные настройки находятся в `config/config.json`:

### Критерии поиска
```json
{
  "search_criteria": {
    "max_deadhead_miles": 250,
    "min_total_miles": 200,
    "min_profitability": 1.5,
    "default_pickup_location": "Chicago, IL",
    "excluded_states": ["CA", "NY"]
  }
}
```

### Настройки мониторинга
```json
{
  "monitoring": {
    "fast_scan_interval_seconds": 2,
    "adaptive_scanning": true,
    "max_concurrent_requests": 3,
    "screenshot_on_error": true
  }
}
```

### Настройки браузера
```json
{
  "browser": {
    "headless": true,
    "timeout_seconds": 30,
    "disable_images": true
  }
}
```

## 📊 Функциональность

### 🔍 Мониторинг грузов
- Сканирование каждые 2-5 секунд
- Параллельная обработка нескольких страниц
- Адаптивная частота сканирования
- Обнаружение новых грузов в реальном времени

### 💰 Расчет прибыльности
- Минимальная ставка: `(deadhead + miles) * 1.7`
- Фильтрация по критериям:
  - Максимум 250 миль deadhead
  - Минимум 200 миль общее расстояние
  - Минимальная прибыльность 1.5x
- Исключение нежелательных состояний

### 📱 Telegram уведомления
- Красивые сообщения с эмодзи
- Детальная информация о грузе
- Приоритизация по прибыльности
- Защита от дубликатов
- Лимиты на количество уведомлений

### 🛡️ Система надежности
- Автоматическое восстановление при ошибках
- Обработка 2FA авторизации
- Обход Cloudflare защит
- Ротация User-Agent
- Сохранение и восстановление сессий

### 📈 Мониторинг производительности
- Отслеживание времени сканирования
- Мониторинг системных ресурсов
- Адаптивная оптимизация
- Детальные отчеты

## 📁 Структура проекта

```
schneider_parser/
├── main.py                    # Главный файл приложения
├── requirements.txt           # Зависимости Python
├── config/
│   ├── config.json           # Основная конфигурация
│   ├── .env                  # Секретные данные
│   └── env_template.txt      # Шаблон .env файла
├── src/
│   ├── auth/
│   │   └── schneider_auth.py # Авторизация Schneider
│   ├── parser/
│   │   ├── load_parser.py    # Парсинг грузов
│   │   └── load_monitor.py   # Мониторинг
│   ├── telegram/
│   │   └── telegram_bot.py   # Telegram уведомления
│   ├── database/
│   │   └── db_manager.py     # Управление БД
│   └── utils/
│       ├── logger.py         # Логирование
│       ├── rate_calculator.py # Расчет прибыльности
│       ├── performance_monitor.py # Мониторинг производительности
│       └── error_handler.py  # Обработка ошибок
├── data/                     # База данных SQLite
├── logs/                     # Файлы логов
├── screenshots/              # Скриншоты для отладки
└── README.md
```

## 🔧 Команды управления

### Остановка приложения
```bash
# Ctrl+C для корректного завершения
# Или отправка сигнала SIGTERM
```

### Просмотр логов
```bash
# Логи в реальном времени
tail -f logs/schneider_parser_$(date +%Y%m%d).log

# Поиск ошибок
grep "ERROR" logs/schneider_parser_*.log
```

### Очистка данных
```bash
# Очистка старых логов
find logs/ -name "*.log" -mtime +7 -delete

# Очистка скриншотов
find screenshots/ -name "*.png" -mtime +1 -delete
```

## 📊 Статистика и отчеты

### Ежедневные отчеты
- Количество найденных грузов
- Количество отправленных уведомлений
- Средняя прибыльность
- Статистика ошибок
- Время работы системы

### Отчеты о производительности
- Время сканирования страниц
- Использование CPU и памяти
- Успешность операций
- Рекомендации по оптимизации

## 🛠️ Устранение неполадок

### Проблемы с авторизацией
1. Проверьте правильность email и пароля
2. Убедитесь, что 2FA настроен правильно
3. Проверьте, не заблокирован ли IP

### Проблемы с Telegram
1. Проверьте токен бота
2. Убедитесь, что бот добавлен в группу
3. Проверьте Chat ID

### Проблемы с производительностью
1. Увеличьте интервал сканирования
2. Уменьшите количество параллельных запросов
3. Проверьте системные ресурсы

### Проблемы с парсингом
1. Проверьте селекторы в коде
2. Убедитесь, что сайт не изменился
3. Проверьте логи на наличие ошибок

## 🔒 Безопасность

- Все секретные данные хранятся в `.env` файле
- Файл `.env` добавлен в `.gitignore`
- Используется ротация User-Agent
- Обработка Cloudflare защит
- Лимиты на количество запросов

## 📝 Логирование

Система ведет подробные логи:
- Цветной вывод в консоль
- Ротация файлов логов
- Разные уровни логирования
- Логирование производительности
- Логирование ошибок

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в папке `logs/`
2. Запустите в тестовом режиме: `python main.py --test`
3. Проверьте конфигурацию в `config/config.json`
4. Убедитесь, что все зависимости установлены

## 📄 Лицензия

Этот проект предназначен для личного использования. Используйте на свой страх и риск.

## ⚠️ Отказ от ответственности

Автор не несет ответственности за:
- Блокировку аккаунта Schneider
- Нарушение условий использования сайта
- Потерю данных или прибыли
- Любые другие последствия использования

Используйте парсер ответственно и в соответствии с условиями использования Schneider FreightPower.
